services:
  x_api:
    container_name: x_api
    restart: always
    image: x_api:latest
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      TZ: Europe/Kiev
    env_file:
      - .env